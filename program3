#include <studio.h>

int main() {
  int counter
  scanf("Ile liczb:" );
  printf("%d",&counter);
  int tab[counter]

  for(int i=0; i<counter; i=i+1) {
    printf("Podaj %d liczbe: \n", (i+1));
      printf("%d",tab[i] );

  }
  return 0;
}
